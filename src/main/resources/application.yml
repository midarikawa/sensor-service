spring:
  datasource:
    url: jdbc:mysql://localhost:4000/sensors
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081